make bash posix work
	- try and make it responsive (weid delay and cursor visibility)
	- make better backspace handling when the previous line is not full
	- now basic ctrl -c works, tinker with events and output so yes does not block the terminal
	- make a fill and flush logic for output reading and operation rendering
	- try and find edge cases
	- have the code to the coding style

make zsh work
make vim work
